# slurmd on cgroup/v2 expects systemd to create its scope. There is no systemd
# in the container, so tell it to go direct. The entrypoint additionally
# creates the directory systemd would have made.
CgroupPlugin=cgroup/v2
IgnoreSystemd=yes
IgnoreSystemdOnFailure=yes
